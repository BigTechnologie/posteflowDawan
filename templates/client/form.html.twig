{% extends 'base.html.twig' %}
{% block body %}
	<h1>Formulaire Clients</h1>
	<div class='card p-3'>
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class='btn btn-dark mt-3'>Enregistrer</button>
		<a class='btn btn-link mt-3' href='{{ path('app_client_index') }}'>Retour</a>
		{{ form_end(form) }}
	</div>
{% endblock %}
